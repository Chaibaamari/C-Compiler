import ISIL.lang;

Programme L3_ISIL_A
PDec

final Float c = 2.3; // Const Finel 
Integer a = 7.5 | m; # chaiba amari
Float  b | x | y; /*
kja urrg hello the variable b and c and y 
hef */
Integer Tab[3] = [1,2,3];
final Integer d = 10;
Integer E = c;
Integer i;

InDebut

y <- (1 * x) / 0;   
m <- (x + 7) * (-1); 
d <- 2;

FOR(i <- 0 ; i < 5 ; i++)
 DO
 x <- 1;
  # add here sous FOR BLOCKS 
     FOR(a <- 0 ; a < 3 ; a++)
      DO
      b <- 4 + b;
      IF( i <=3) DO
         b <- 4 + b;
       ELSE
         b <- 11+b;
       ENDIF
      Input("%d",x); // formatage non valid
      Write("Hello world chaiba %d", x);
     ENDFOR
ENDFOR

IF( i <=3) 
DO
 Write("the values is %d" , s);
ENDIF

InFin


